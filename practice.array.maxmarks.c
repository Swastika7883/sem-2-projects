#include<stdio.h>
#define size 5
void main(){
    int i;
    int max=0;
    int marks[size];

    for(i = 0; i<size ; i++)
    {printf("Enter the marks of %d student =" , i+1);
    scanf("%d", &marks[i]);
    }
    for (i=0; i)
}